# Finish v0.1.1 Batch-One Honesty/Bleed Fixes

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land the already-coded v0.1.1 batch (B1/B2/H2/H4/M3/M4) as reviewable commits, fill the remaining acceptance gaps (ruff, CI non-editable smoke, README loud-degradation notes), without starting batch-two architecture work.

**Architecture:** Working tree already contains the four batch-one deliverables from `.issue/2026-07-27-11_45-v0_1-issue-diagnosis-and-fix-plan.md` §6. This plan finishes packaging and the documented acceptance criteria that are still missing.

**Tech Stack:** Python 3.10+, pytest, ruff, GitHub Actions CI.

## Global Constraints

- Pure fixes only; no P0 density contract / P1 beats / H1 fingerprint split (batch two).
- Minimal commits: one logical PR-shaped commit each (CJK, generate packaging, timing.json, loud degradation + docs/CI).
- Conventional Commits, English messages.
- Do not commit `three-body.txt`.
- Verify: `.venv/bin/ruff check` on touched files, `.venv/bin/ruff format` on touched files, `.venv/bin/python -m pytest -q`.

## Progress vs diagnosis (as of WIP on main)

| Item | Status |
|------|--------|
| B1 CJK fonts (`core/comic/fonts.py` + layout + tests) | Code present, uncommitted |
| B2 `core/cli_generate.py` + thin examples + CLI import | Code present, uncommitted; **CI non-editable smoke missing** |
| H2 `timing.json` in web server | Code present, uncommitted |
| H4/M3/M4 loud degradation | Code present, uncommitted; **README notes missing** |
| Ruff on WIP | **2 lint errors** (unused `source`, E501) |
| Batch two (H1, M1, M2, H3, M5, L1/M6) | Not started |

## File map (already created/modified; do not re-implement)

| File | Role |
|------|------|
| `core/comic/fonts.py` | CJK font resolution |
| `core/comic/layout.py` | Use fonts + webtoon pixel guard |
| `core/comic/export.py` | manga2pdf warning |
| `core/cli_generate.py` | Packaged generate implementation |
| `core/cli.py` | Import `run_generate` from core |
| `examples/generate_comic.py` | Thin wrapper |
| `web/server.py` | timing.json + bind warning |
| `tests/test_layout_cjk.py`, `test_cli_generate.py`, `test_loud_degradation.py`, `test_web_server.py` | Coverage |
| `.github/workflows/ci.yml` | **Add** non-editable smoke step |
| `README.md` | **Add** font / manga2pdf / bind security notes |

---

### Task 1: Lint-clean the WIP

**Files:** `core/comic/fonts.py`, `tests/test_loud_degradation.py`

- [ ] Fix unused `source` → `_source` in fonts.py candidate loop
- [ ] Break E501 line in `test_loud_degradation.py`
- [ ] Run ruff check/format on WIP files; expect clean
- [ ] Do not commit alone — fold into the relevant feature commits below

### Task 2: Commit B1 (CJK)

```bash
git add core/comic/fonts.py core/comic/layout.py tests/test_layout_cjk.py
# layout also has webtoon guard — if mixed, split: fonts+layout font wiring in this commit,
# webtoon guard lines go with Task 5. Prefer one layout commit with B1 if hard to split;
# acceptable: commit fonts + full layout.py with B1 message noting webtoon guard rides along,
# OR use git add -p. Prefer clean split via add -p when practical.
git commit -m "fix(layout): render CJK dialogue with discoverable fonts"
```

Acceptance: CJK tests pass; tofu regression test present.

### Task 3: Commit B2 (generate packaging)

```bash
git add core/cli_generate.py core/cli.py examples/generate_comic.py tests/test_cli_generate.py
git commit -m "fix(cli): move generate implementation into core for pip installs"
```

### Task 4: Commit H2 (timing.json)

```bash
git add web/server.py tests/test_web_server.py
git commit -m "fix(web): persist job elapsed time in timing.json"
```

### Task 5: Loud degradation + README + CI smoke

- [ ] Ensure webtoon guard / export warning / bind warning are committed (with layout/export if not already)
- [ ] README: short notes for `INKSTONE_FONT_PATH`, manga2pdf optional, non-loopback UI bind risk
- [ ] CI: after install, add a job or step:
  ```yaml
  - name: Non-editable install smoke
    run: |
      python -m venv /tmp/inkstone-smoke
      /tmp/inkstone-smoke/bin/pip install .
      /tmp/inkstone-smoke/bin/inkstone --help
      /tmp/inkstone-smoke/bin/inkstone plan --book examples/sample_novel.txt
  ```
  (Adjust CLI flags to match actual `inkstone plan` argparse.)
- [ ] Commit: `fix: loud degradation for manga2pdf, webtoon limit, and UI bind` and/or `ci: smoke non-editable inkstone install` as separate minimal commits if cleaner

### Task 6: Verify + hand off

- [ ] Full pytest + ruff
- [ ] Branch `fix/v0.1.1-bleed-fixes` from current main (do not land raw on main without PR)
- [ ] Present PR option to user (do not push unless asked)

## Out of scope (batch two — next plan)

H1 fingerprint split, M1 chapter regex, M2 config centralization, H3 job TTL, M5 to_thread, L1/M6 language/deps.
